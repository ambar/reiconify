{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test"
  },
  "devDependencies": {
    "es-jest": "^0.0.1",
    "jest": "^26.6.3",
    "lerna": "^3.4.3",
    "react": "^16.0.1",
    "react-test-renderer": "^16.8.4",
    "rimraf": "^2.6.3"
  }
}
