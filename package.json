{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "test": "yarn workspaces run test"
  },
  "devDependencies": {
    "es-jest": "^2.0.0",
    "jest": "^29.2.1",
    "react": ">=16.14.0",
    "react-test-renderer": ">=16.14.0",
    "rimraf": "^2.6.3"
  }
}
